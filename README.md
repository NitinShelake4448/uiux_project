
  # Provide requested design

  This is a code bundle for Provide requested design. The original project is available at https://www.figma.com/design/NTeZq8EvQB5ChvA4tt30Qa/Provide-requested-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  